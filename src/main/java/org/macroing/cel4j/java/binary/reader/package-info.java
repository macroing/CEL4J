/**
 * Provides classes and interfaces for reading {@code ClassFile}s from binary data.
 * <p>
 * Included are also Service Provider Interface (SPI) classes and interfaces for reading {@code AttributeInfo}s and {@code CPInfo}s.
 */
package org.macroing.cel4j.java.binary.reader;